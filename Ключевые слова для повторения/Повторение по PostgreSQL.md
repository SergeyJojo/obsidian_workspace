
Отлично! Ниже — **расширенные блоки по PostgreSQL** с разбивкой по темам и акцентом на то, **что важно повторить перед собеседованием**. Эти блоки ориентированы на **глубокое понимание, практическое применение и объяснение с примерами**.

---

## 🔍 **1. План выполнения запроса**

### Что важно:

- Как читать `EXPLAIN (ANALYZE, BUFFERS)`
    
- Что означают поля: `actual time`, `rows`, `loops`, `cost`
    
- Как планировщик выбирает между `Seq Scan`, `Index Scan`, `Bitmap Scan`
    

### Вопросы к себе:

- Почему `Index Scan` может быть медленнее `Seq Scan`?
    
- Что такое **row estimate mismatch**?
    
- Что значит `Rows Removed by Filter`?
    

---

## 📚 **2. Индексы и их поведение**

### Типы индексов:

- `B-Tree`: по умолчанию
    
- `GIN`/`GiST`: для full-text search, массивов, jsonb
    
- `BRIN`: для колонок с коррелированными значениями
    
- `Hash`: с ограниченным применением
    

### Что повторить:

- Как создать partial, covering, expression index
    
- Когда индекс **не используется** (функции, типы, селективность)
    
- Как проверить `pg_stat_user_indexes` на "мертвые" индексы
    

### Вопросы:

- Почему индекс не срабатывает на `LOWER(col)`?
    
- Что такое index-only scan? Как это проверить?
    

---

## 🔄 **3. Транзакции и блокировки**

### Уровни изоляции:

- `Read Uncommitted`, `Read Committed` (по умолчанию), `Repeatable Read`, `Serializable`
    
- MVCC: как работает, какие бывают эффекты (phantom, non-repeatable read)
    

### Блокировки:

- Row-level: `SELECT FOR UPDATE`, `NOWAIT`, `SKIP LOCKED`
    
- Table-level: `LOCK TABLE`
    
- Deadlocks, `pg_locks`
    

### Что спросить себя:

- Что делает `vacuum` с заблокированными строками?
    
- Чем опасны долгие транзакции?
    

---

## 💥 **4. Автоматическая очистка и VACUUM**

### Что повторить:

- `autovacuum`: что он делает, когда запускается
    
- `pg_stat_user_tables` — какие поля смотреть
    
- `dead tuples`, `n_tup_ins`, `n_tup_upd`, `n_tup_del`
    

### Вопросы:

- Когда `autovacuum` не успевает?
    
- Как решить bloat?
    
- Что такое `freezing`?
    

---

## 📈 **5. Производительность, статистика, буферы**

### Метрики:

- `pg_stat_statements`
    
- `shared_blks_hit`, `shared_blks_read`
    
- Что такое hit ratio и как его интерпретировать
    

### Буферы:

- Что такое shared buffers, work_mem, temp_buffers
    
- Когда создаётся temp file?
    
- Как измерить cache hit ratio?
    

---

## 📦 **6. Работа с JSONB**

### Что повторить:

- `->`, `->>`, `#>`, `#>>`, `@>`, `?`, `?|`
    
- Индексы по JSONB (`GIN`)
    
- Примеры запросов с фильтрацией и агрегацией
    

### Вопросы:

- Как ускорить поиск по JSONB-массиву?
    
- Как работает `jsonb_path_ops`?
    

---

## 🧬 **7. CTE, оконные функции, агрегации**

### Что повторить:

- `WITH` (CTE) + `RECURSIVE`
    
- `ROW_NUMBER()`, `RANK()`, `PARTITION BY`, `LAG()`, `LEAD()`
    
- Группировка, `GROUPING SETS`, `ROLLUP`, `CUBE`
    

### Вопросы:

- В чём разница между обычным CTE и `materialized`?
    
- Как сделать агрегат на уровне групп и строк одновременно?
    

---

## 🔧 **8. Сервисные функции и диагностика**

### Повторить:

- `pg_stat_activity`, `pg_locks`, `pg_stat_replication`
    
- `pg_current_wal_lsn`, `pg_wal_lsn_diff`
    
- Как искать медленные запросы через `pg_stat_statements`
    

### Вопросы:

- Как найти и прервать зависший запрос?
    
- Как узнать, сколько места занимает таблица?
    

---

## 🔁 **9. Репликация и отказоустойчивость**

### Повторить:

- WAL, LSN, WAL archive
    
- Physical vs Logical Replication
    
- `pg_basebackup`, `recovery.conf` (или postgresql.auto.conf в новых версиях)
    

### Вопросы:

- Что такое `replication slot`?
    
- Как отслеживать задержку реплики?
    

---

## 📜 **10. Полезные утилиты и флаги**

### Инструменты:

- `psql`: `\dt`, `\x`, `\df+`, `\l+`, `\watch`
    
- `pg_stat_activity`, `pg_stat_database`, `pg_stat_user_indexes`
    
- `pg_repack`, `pgbouncer`, `pgbadger`
    

---

## 📌 Хочешь?

Я могу:

- Сделать **карточки для быстрого активного повторения** по этим темам.
    
- Подготовить **10 практических вопросов уровня собеседования**.
    
- Дать **сложный кейс из продакшена и попросить разобрать**.
    

Какой формат удобнее: карточки, вопросы, мини-собеседование?