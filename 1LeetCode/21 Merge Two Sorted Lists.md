
Вот полное разбор и решение задачи **LeetCode #21 — Merge Two Sorted Lists** с пояснениями на Go.

---

## 🧩 Условие задачи 21. Merge Two Sorted Lists

> Даны два отсортированных по возрастанию связанных списка `l1` и `l2`. Объедините их в один отсортированный связный список и верните **голову** нового списка.

### Пример

```txt
Input:  l1 = [1,2,4], l2 = [1,3,4]
Output: [1,1,2,3,4,4]
```

---

## 📚 Подход (итеративный)

1. Заводим **вспомогательную фейковую голову** `dummy`, чтобы упростить сборку списка.
    
2. Используем указатель `current`, чтобы добавлять элементы.
    
3. Сравниваем `l1.Val` и `l2.Val`, меньший кладём в результат.
    
4. Когда один из списков закончится — прицепляем оставшуюся часть другого.
    

---

## ✅ Решение на Go

```go
type ListNode struct {
    Val  int
    Next *ListNode
}

func mergeTwoLists(l1 *ListNode, l2 *ListNode) *ListNode {
    dummy := &ListNode{}
    current := dummy

    for l1 != nil && l2 != nil {
        if l1.Val < l2.Val {
            current.Next = l1
            l1 = l1.Next
        } else {
            current.Next = l2
            l2 = l2.Next
        }
        current = current.Next
    }

    // Прицепляем оставшийся хвост
    if l1 != nil {
        current.Next = l1
    } else {
        current.Next = l2
    }

    return dummy.Next
}
```

---

## 🧠 Пояснение к коду

| Компонент                | Назначение                                              |
| ------------------------ | ------------------------------------------------------- |
| `dummy`                  | Фейковый узел, чтобы избежать проверок на пустую голову |
| `current`                | Указатель, куда добавляем следующий узел                |
| `l1.Val < l2.Val`        | Сравниваем значения из двух списков                     |
| `current.Next = ...`     | Добавляем наименьший элемент к результату               |
| `current = current.Next` | Перемещаем указатель                                    |
| `current.Next = l1/l2`   | Прицепляем оставшиеся узлы                              |

---

## 🔄 Альтернатива — рекурсивное решение

```go
func mergeTwoLists(l1 *ListNode, l2 *ListNode) *ListNode {
    if l1 == nil {
        return l2
    }
    if l2 == nil {
        return l1
    }
    if l1.Val < l2.Val {
        l1.Next = mergeTwoLists(l1.Next, l2)
        return l1
    } else {
        l2.Next = mergeTwoLists(l1, l2.Next)
        return l2
    }
}
```

> Рекурсия лаконичнее, но может переполнить стек при длинных списках.

---

## ⏱ Сложность

- **Время**: `O(n + m)` — проходим оба списка один раз.
    
- **Память**:
    
    - Итеративное: `O(1)` — кроме результата, не используем доп. памяти.
        
    - Рекурсивное: `O(n + m)` — из-за глубины стека вызовов.
        

---

Если хочешь, могу сразу перейти к задаче 23 (Merge K Sorted Lists) или показать пример тестов на эту задачу.