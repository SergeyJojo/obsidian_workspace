Вот отформатированный и аккуратно оформленный код на Go с учётом твоих замечаний и рекомендаций. Я не стал внедрять всё (graceful shutdown, env, log и т.п.) — это можно вынести в архитектурный рефакторинг, если хочешь. Пока — просто **приведённый в порядок код**:

---

### ✅ Форматированный код:

```go
package main

import (
	"database/sql"
	"fmt"
	"io"
	"net/http"
	"os"
	"strconv"
	"strings"

	_ "github.com/lib/pq"
)

func main() {
	if len(os.Args) == 2 {
		cmdName := os.Args[1]
		if cmdName == "help" {
			fmt.Println("Usage: ./currency update")
		} else if cmdName == "update" {

			urlsBank := []struct {
				bankName string
				curFrom  string
				curTo    string
				url      string
			}{
				{
					bankName: "Bank 1",
					curFrom:  "RUB",
					curTo:    "USD",
					url:      "http://bank.example.com/rates/rub-usd",
				},
				{
					bankName: "Bank 2",
					curFrom:  "RUB",
					curTo:    "USD",
					url:      "http://bank2.example.com/rates?currFrom=RUR&currTo=USD",
				},
			}

			clientBank := &http.Client{}

			for _, url := range urlsBank {
				req, _ := http.NewRequest(http.MethodGet, url.url, nil)

				if url.bankName == "Bank 2" {
					req.Header.Add("Authorization", `auth_token="XXXXXXX"`)
				}

				resp, err := clientBank.Do(req)
				if err != nil {
					panic(err)
				}
				defer resp.Body.Close()

				body, _ := io.ReadAll(resp.Body)
				strBody := string(body)

				if url.bankName == "Bank 1" {
					strBody = strings.ReplaceAll(strBody, ",", ".")
				}

				value, err := strconv.ParseFloat(strBody, 64)
				if err != nil {
					panic(err)
				}

				err = updateCurrency(url.bankName, url.curFrom, url.curTo, value)
				if err != nil {
					panic(err)
				}
			}
		}
	} else {
		fmt.Println("Usage: ./currency update")
	}
}

const (
	host     = "localhost"
	port     = 5432
	user     = "postgres"
	password = "<password>"
	dbname   = "<dbname>"
)

func updateCurrency(bank, from, to string, value float64) error {
	psqlconn := fmt.Sprintf(
		"host=%s port=%d user=%s password=%s dbname=%s sslmode=disable",
		host, port, user, password, dbname,
	)

	db, err := sql.Open("postgres", psqlconn)
	CheckError(err)
	defer db.Close()

	err = db.Ping()
	CheckError(err)

	fmt.Println("Connected!")

	query := `INSERT INTO currency_rates ("bank", "from", "to", "value") VALUES ($1, $2, $3, $4)`
	_, err = db.Exec(query, bank, from, to, value)

	return err
}

func CheckError(err error) {
	if err != nil {
		panic(err)
	}
}
```

---

## 🔧 Что было улучшено:

| Что                         | Было                               | Стало                                                                                                |
| --------------------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------- |
| ✅ Отступы                   | Неровные, лишние пустые строки     | Выравнено и структурировано                                                                          |
| ✅ SQL                       | Форматировался через `fmt.Sprintf` | Заменено на `Exec(..., ..., ...)` с аргументами (без SQL-инъекций)                                   |
| ✅ Конкатенации              | Были ручные                        | Приведено к единому стилю                                                                            |
| ✅ Комментарии               | Были дубли, в коде искажение       | Удалены/сжаты, можно добавить позже документацию                                                     |
| ✅ `defer resp.Body.Close()` | вызывался в цикле (нормально)      | оставлено как есть, но мог бы быть улучшен с `io.Copy(io.Discard, resp.Body)` для быстрой утилизации |

---

## ⚠️ Что всё ещё требует архитектурной переработки:

- ⚠️ **graceful shutdown** (через context + сигнал)
    
- ⚠️ **логирование** (`log/slog` или `zap`)
    
- ⚠️ **не открывать соединение к БД каждый раз**
    
- ⚠️ **не передавать пароль/хост напрямую — использовать `.env`, Vault и т.д.**
    
- ⚠️ **параллельная отправка запросов** (goroutines + семафор или errgroup)
    

---

Хочешь, я могу поэтапно переписать это в виде **"промышленного решения"**, с логами, конфигами, graceful shutdown, и пулом соединений к PostgreSQL.