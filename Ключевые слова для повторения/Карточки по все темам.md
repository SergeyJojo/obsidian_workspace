
Вот подробный и активный чеклист по ключевым темам, разбитый по направлениям — для повторения перед собеседованием:

---

## 🐧 Linux

### 📂 Процессы и системные вызовы

- `fork`, `exec`, `wait`, `exit`
    
- Разница `vfork` vs `fork`
    
- Что такое `strace`, как им пользоваться
    
- Как посмотреть системные вызовы процесса
    

### 🧠 Память и управление

- Что такое `brk`, `mmap`, `heap`, `stack`
    
- Страницы памяти, page faults
    
- `top`, `htop`, `ps`, `smem`
    

### 🔁 Контекст переключения

- Что сохраняется при переключении контекста
    
- Как увидеть количество context switches (`vmstat`, `pidstat`)
    

### 📦 Файловая система и I/O

- Разница `read` vs `mmap`
    
- Разница `O_DIRECT` и буферизованного ввода-вывода
    
- Что такое inode, dentry, VFS
    

### 📡 Сети

- `netstat`, `ss`, `lsof`, `tcpdump`
    
- TCP 3-way handshake
    
- `epoll`, `select`, `poll` — сравнение
    
- Как работает `iptables`, `traceroute`
    

---

## 🐘 PostgreSQL

### 🔍 План выполнения запроса

- `EXPLAIN (ANALYZE, BUFFERS)` — как читать
    
- Разница `Index Scan`, `Bitmap Heap Scan`, `Seq Scan`
    
- Что такое selectivity, cost estimation
    

### 🧮 Индексы и оптимизация

- B-Tree vs Hash vs GIN
    
- Partial index, covering index, expression index
    
- Что такое bloated index, `pg_repack`
    

### 🔄 Транзакции и блокировки

- Уровни изоляции (read committed, repeatable read, serializable)
    
- `SELECT FOR UPDATE`, `NOWAIT`, `SKIP LOCKED`
    
- `pg_locks`, `deadlocks`, `vacuum`
    

### 🔁 Репликация и WAL

- Как работает WAL
    
- logical vs physical replication
    
- Что такое `checkpoint`, `bgwriter`, `autovacuum`
    

---

## 🧵 Golang: Многопоточность и синхронизация

### ⚙ Goroutines

- Что происходит при вызове `go f()`
    
- Как планируются горутины (M:N model)
    
- Stack growth, preemption
    

### 📬 Каналы

- Синхронные и буферизированные
    
- Что будет при закрытии канала
    
- `select`, `default`, `time.After`
    

### 🔒 Sync-примитивы

- `sync.Mutex`, `RWMutex`, `WaitGroup`, `Cond`
    
- `sync.Map`, зачем нужен
    
- `atomic.Value`, `CompareAndSwap`, `AddInt64`
    

### 🧼 Контекст

- `context.Background`, `WithCancel`, `WithTimeout`
    
- Где важно передавать `ctx` в продакшене
    

---

## 📊 Профилирование и отладка

### 🔬 CPU / Memory profiling

- `pprof` — как запускать, `curl :6060/debug/pprof`
    
- `go tool pprof`, `top`, `list`, `web`
    

### 🔥 Tracing

- `runtime/trace`, `go tool trace`
    
- Визуализация времени выполнения
    

### 📦 Инструменты

- `dlv` — отладка
    
- `benchstat`, `testing.Benchmark`
    

---

## 📐 Алгоритмы (только must-know минимум)

### 🔁 Структуры данных

- Map, Set (через `map[Type]struct{}` в Go)
    
- Stack, Queue, Priority Queue (heap)
    

### 📈 Сложность

- O(1), O(log n), O(n), O(n log n), O(n^2)
    
- Где ты "влетаешь" по памяти/времени
    

### 🎯 Часто используемые алгоритмы

- Двоичный поиск, two pointers, sliding window
    
- BFS / DFS — руками на графе
    
- Topological sort, Union-Find, Dijkstra
    

---

## ✅ Как повторять

Для каждого блока:

- Задавай себе **вопрос из головы**
    
- Пробуй **написать код** или **объяснить словами**
    
- Пиши короткий **листик с "белыми пятнами"**, и через 1–2 часа к ним вернись
    

---

Если хочешь, могу:

- Сделать **карточки (active recall) по этим темам**
    
- Составить **план на 2 дня**
    
- Или устроить **мини-собеседование** по Go/Postgres
    

Скажи, что из этого тебе сейчас нужнее.