Вот твой код, Сергей, я ничего не менял, только добавил комментарии на то, что сто́ит улучшить или поправить:

```go
package main

import (
	"database/sql"
	"fmt"
	"io"
	"net/http"
	"os"
	"strconv"
	"strings"

	_ "github.com/lib/pq"
)

// ТЗ: мы хотим собирать информацию по курсам валют в разных банках.
// Требуется написать программу, которая каждую минуту будет отправлять запрос в банк и получать курс нескольких валют и сохранять результат в БД.
// Банков может быть несколько.

func main() {

	if len(os.Args) == 2 {
		cmdName := os.Args[1]
		if cmdName == "help" {
			fmt.Println("Usage is './currency update'")
			
		} else if cmdName == "update" {

			urlsBank := []struct { 
				bankName string
				curFrom  string
				curTo    string
				url      string
			}{
				{
					bankName: "Bank 1",
					curFrom: "RUB",
					curTo:   "USD",
					url:     "http://bank.example.com/rates/rub-usd",
				},
				{
					bankName: "Bank 2",
					curFrom: "RUB",
					curTo:   "USD",
					url:     "http://bank2.example.com/rates?currFrom=RUR&currTo=USD",
				},
			}

			clientBank := &http.Client{} // 👉 стоит добавить таймаут, иначе может виснуть навсегда

			for _, url := range urlsBank { 

				req, _ := http.NewRequest(http.MethodGet, url.url, nil) 
				// 👉 игнорирование ошибки из NewRequest плохо, лучше обработать

				if url.bankName == "Bank 2" {
					req.Header.Add("Authorization", "auth_token=\"XXXXXXX\"") // 👉 токен вынести в конфиг/секрет
				}
                
				resp, err := clientBank.Do(req)
				if err != nil {
					panic(err) // 👉 лучше логировать и продолжить по другим банкам, а не падать полностью
				}
				defer resp.Body.Close() // 👉 defer внутри цикла приведёт к накоплению открытых соединений, лучше закрывать сразу после использования

				body, _ := io.ReadAll(resp.Body) // 👉 игнор ошибки здесь неправильно, нужно проверять

				strBody := string(body)

				if url.bankName == "Bank 1" {
					strBody = strings.ReplaceAll(strBody, ",", ".") // 👉 лучше парсить JSON/XML, чем строку "на глаз"
				}

				value, err := strconv.ParseFloat(strBody, 64)
				if err != nil {
					panic(err) // 👉 не паниковать, а обработать/логировать
				}
				// 👉 ошибка: ты передаёшь curFrom дважды, а curTo игнорируешь
				err = updateCurrency(url.bankName, url.curFrom, url.curFrom, value) 
				if err != nil {
					panic(err) // 👉 не паниковать
				}
			}
		}	
	} else {
		fmt.Println("Usage is './currency update'")
	}
}

const ( // 👉 вынести в конфиг/переменные окружения (ENV), а не хардкодить в коде
	host     = "localhost"
	port     = 5432
	user     = "postgres"
	password = "<password>" 
	dbname   = "<dbname>"
)

func updateCurrency(bank, from, to string, value float64) error { 
	psqlconn := fmt.Sprintf(
		"host=%s port=%d user=%s password=%s dbname=%s sslmode=disable", 
		host, port, user, password, dbname,
	)

	db, err := sql.Open("postgres", psqlconn)
	CheckError(err) // 👉 открывать соединение к БД на каждый insert — плохо, лучше инициализировать один раз в main()

	defer db.Close()

	err = db.Ping() // 👉 избыточно дергать Ping() при каждом запросе
	CheckError(err)

	fmt.Println("Connected!") // 👉 в проде лучше логировать через логгер

	// 👉 строка собирается через fmt.Sprintf → возможна SQL-инъекция
	insertStmt := fmt.Sprintf(
		`insert into currency_rates ("bank", "from", "to", "value") values('%s', '%s', '%s', '%.2f')`, 
		bank, from, to, value,
	)
	_, err = db.Exec(insertStmt) 
	// 👉 лучше использовать prepared statements или Exec с плейсхолдерами ($1, $2, ...)

	return err
}

func CheckError(err error) {
	if err != nil {
		panic(err) // 👉 злоупотребление panic, лучше возвращать ошибку вверх и логировать
	}
}
```

---

⚡ Кратко, что нужно улучшить:

1. **Ошибка в логике:** ты передаёшь `curFrom` вместо `curTo` в `updateCurrency`.
    
2. **DB connection:** нельзя открывать и закрывать БД на каждый запрос, лучше держать пул соединений (`sql.DB` именно для этого).
    
3. **SQL-инъекции:** использовать `Exec("INSERT ... VALUES ($1, $2, ...)", args...)`, а не `fmt.Sprintf`.
    
4. **panic:** заменить на нормальную обработку ошибок.
    
5. **defer в цикле:** закрывать `resp.Body` сразу после чтения, а не откладывать.
    
6. **Config:** вынести пароли и параметры БД в конфиг/ENV.
    
7. **HTTP:** добавить `Timeout` в `http.Client`, проверять ошибки от `NewRequest` и `ReadAll`.
    
8. **Парсинг ответа:** сейчас «на глаз», лучше парсить реальный формат (JSON/XML).
    

---

Хочешь, я перепишу этот код в **более правильном виде с пулом БД, логированием и нормальной обработкой ошибок**, но сохранив твою логику?